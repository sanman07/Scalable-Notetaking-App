apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  labels:
    app: notes-frontend
spec:
  selector:
    app: notes-frontend
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP 