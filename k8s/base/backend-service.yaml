apiVersion: v1
kind: Service
metadata:
  name: backend-service
  labels:
    app: notes-backend
spec:
  selector:
    app: notes-backend
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  type: ClusterIP 